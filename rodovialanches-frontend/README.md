# Rodovia Lanches Frontend

Instalação e execução:

```bash
cd "Nova pasta/rodovialanches-frontend"
npm install
npm run dev
```

Abra `http://localhost:5173` por padrão.

O frontend espera a API em `http://localhost:8080`. Para alterar, ajuste `VITE_API_URL`.
